model UploadedFile {
    id String @id @default(cuid())

    bucket   String
    region   String
    key      String
    eTag     String
    provider String

    locked    Boolean  @default(false)
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    LogoFile Company[] @relation(name: "logoFile")
}
